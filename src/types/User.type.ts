export type UserProfileType = {
  name: string;
  email: string;
  password: string; // Typically, you should handle passwords securely and not display them in plaintext
  phone: string;
  role: string;
  address: string;
};
